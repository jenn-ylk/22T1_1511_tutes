// A program that uses loops to count
// Tutorial week 3, COMP1511

#include <stdio.h>

int main (void) {

    int end;
    printf("Enter finish: ");
    scanf("%d", &end);
    
    int i = 1;
    while (i <= end) {
        printf("%d\n", i);
        i++;
    }

    return 0;
}
