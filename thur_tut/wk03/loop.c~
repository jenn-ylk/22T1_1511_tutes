// Testing out some broken while loops
// this program is specifically to discuss some loops, and how to fix them
// Tutorial week 3, COMP1511

#include <stdio.h>

int main (void) {

    /*
    // initialise variables needed for the loop
    int i = 0;
    // loop condition
    while (i < 10) {
        printf("Hello\n");
        // change at the end of the while loop
        // could also be decreasing a counter, changing a flag
        // (if a condition was met)
        i++;
    }
    */

    // issue: uninitialised value for i
    /*
    int i;

    while (i < 100) {
        printf("%d\n", i);
        i = i + 1;
    }
    */
    
    
    // issue: assignment inside of a conditional statement
    /*
    int i = 0;
    int j = 0;

    while (j = 1 || i < 100) {
        printf("%d\n", i);
        i = i + 1;
    }
    */
    
    // n grows faster than i, so i < n is always true
    /*
    int i = 0;
    int n = 10;

    while (i < n) {
        printf("%d\n", i);
        n = n + i;
        i = i + 1;
    }
    */
    
    //
    /*
    int i = 0;

    while (i < 10)
        printf("%d\n", i);
        i = i + 1;
    */

    return 0;
}
